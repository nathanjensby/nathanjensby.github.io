import React from "react";

const Footer = () => {
  const quote = "I'd rather be rich than stupid.";
  return (
    <div className="mt-36 mx-8">
      <p className="text-xsm text-xs slate-400 mb-4">{quote}</p>
    </div>
  );
};

export default Footer;
